import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'saman.lafortuna',
  appName: 'SAMAN',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
